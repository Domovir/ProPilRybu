# Configuration Examples

This directory contains example configuration files for the Universal RTSP Recorder.

Example files do not contain real credentials.

Production configuration files must be stored outside the Git repository, for example:

/etc/propilrybu/

Never commit production configuration files containing passwords or other sensitive information.